# 🚀 ІНСТРУКЦІЇ З ЗАПУСКУ

## Швидкий Старт

### Варіант 1: Локальний Запуск (Найпростіший)

1. **Відкрийте `index.html` у браузері:**
   ```bash
   # На Windows
   start C:\path\to\client\index.html
   
   # На macOS
   open /path/to/client/index.html
   
   # На Linux
   firefox /path/to/client/index.html
   ```

   Або просто перетягніть файл `index.html` в браузер.

### Варіант 2: Простий HTTP Сервер (Рекомендується)

#### Python 3:
```bash
cd /home/fant/VsCode/web_chat/client
python -m http.server 8000
```
Потім відкрийте: http://localhost:8000

#### Python 2:
```bash
cd /home/fant/VsCode/web_chat/client
python -m SimpleHTTPServer 8000
```

#### Node.js (з http-server):
```bash
# Спочатку встановіть
npm install -g http-server

# Потім запустіть
cd /home/fant/VsCode/web_chat/client
http-server -p 8000
```

#### Node.js (з Live Server):
```bash
npm install -g live-server
cd /home/fant/VsCode/web_chat/client
live-server --port=8000
```

### Варіант 3: У VS Code (Найзручніший)

1. Встановіть розширення "Live Server" (Ritwick Dey)
2. Праворуч на `index.html` → "Open with Live Server"
3. Браузер автоматично відкриється на http://localhost:5500

## 📋 Вимоги

- ✅ Современний браузер (Chrome, Firefox, Safari, Edge)
- ✅ Інтернет (для API запитів на сервер)
- ✅ Сервер повинен запускатися на http://localhost:3000 або замініть URL в `src/js/api.js`

## 🔧 Налаштування API

Перед запуском переконайтесь, що ваш сервер доступний:

### Перевірка Сервера

```bash
curl http://localhost:3000/api/health
```

### Якщо Сервер на Іншому URL

Відредагуйте `src/js/api.js`:

```javascript
// Знайдіть цю лінію
const api = new APIClient('http://localhost:3000/api');

// Змініть на вашу адресу
const api = new APIClient('http://your-server.com:3000/api');
```

## 🧪 Тестування

### 1. Перевірьте, чи все завантажується:
- Відкрийте http://localhost:8000 (або порт, який ви вибрали)
- Повинна з'явитися сторінка логіну з градієнтом фіолет-рожевий

### 2. Перевірте консоль браузера:
- F12 → Console
- Не повинно бути помилок червоного кольору

### 3. Тестуйте форму:
- Натисніть "Register"
- Повинна з'явитися сторінка реєстрації з двома колонками
- Натисніть "Back"
- Повинна з'явитися сторінка логіну

### 4. Тестуйте валідацію:
- Введіть невірний email (наприклад: "notanemail")
- Натисніть Login
- Повинна з'явитися помилка

### 5. Тестуйте API запити:
- Заповніть форму правильно
- Натисніть Login або Register
- Відкрийте F12 → Network
- Повинен бути запит POST на ваш сервер
- Перевірте відповідь сервера

## 🐛 Вирішення Проблем

### Проблема: Сторінка не завантажується

**Можливі причини:**
1. Браузер не може відкрити файл
2. Шляхи до файлів неправильні

**Рішення:**
```bash
# Переконайтесь, що запущений HTTP сервер
cd /home/fant/VsCode/web_chat/client
python -m http.server 8000

# Відкрийте http://localhost:8000
```

### Проблема: Стилі не завантажуються

**Можливі причини:**
1. CSS файли на іншому місці
2. Браузер кешує старий файл

**Рішення:**
1. Очистіть кеш: Ctrl+Shift+R (Windows/Linux) або Cmd+Shift+R (Mac)
2. Перевірте шляхи в `index.html`:
   ```html
   <link rel="stylesheet" href="./src/css/styles.css" />
   ```

### Проблема: API запити не мають відповідей

**Можливі причини:**
1. Сервер не запущений
2. URL сервера невірний
3. CORS не налаштований на сервері

**Рішення:**
1. Перевірте, чи запущений сервер:
   ```bash
   curl http://localhost:3000/api/health
   ```

2. Перевірте URL в `src/js/api.js`

3. На сервері додайте CORS заголовки:
   ```rust
   // Якщо це Rust/Actix
   use actix_cors::Cors;
   
   let cors = Cors::default()
       .allow_any_origin()
       .allow_any_method()
       .allow_any_header();
   ```

### Проблема: JavaScript помилки

**Рішення:**
1. Відкрийте F12 → Console
2. Прочитайте повідомлення про помилку
3. Перевірте логи на сервері
4. Перевірте шляхи до файлів

## 📱 Мобільне Тестування

### Локально:
1. Запустіть сервер: `python -m http.server 8000`
2. Знайдіть IP вашого комп'ютера:
   ```bash
   # Windows
   ipconfig
   
   # Mac/Linux
   ifconfig
   ```
3. На мобільному браузері відкрийте: `http://YOUR_IP:8000`

### На Смартфоні:
- Переконайтесь, що смартфон в одній мережі з комп'ютером
- Відкрийте http://192.168.X.X:8000 (замініть IP)

## 🌐 Production Розгортання

### На GitHub Pages:
1. Закомітьте в Git
2. Активуйте GitHub Pages в налаштуваннях репозиторія
3. Виберіть гілку `main` або `gh-pages`

### На Звичайному Хостингу:
1. Завантажте папку `client` на хостинг
2. Переконайтесь, що `index.html` доступний по основному URL
3. Налаштуйте CORS на сервері API

### На Vercel/Netlify:
1. Закомітьте проект в Git
2. Підключіть репозиторій до Vercel/Netlify
3. Встановіть Build Command: (залишити пусто)
4. Встановіть Publish Directory: `client`

## 📞 Допомога

Якщо у вас виникли проблеми:

1. Перевірте консоль браузера (F12 → Console)
2. Перевірте сторінку Network (F12 → Network)
3. Прочитайте документацію в `README.md` та `SETUP_GUIDE.md`
4. Перевірте шляхи до файлів
5. Переконайтесь, що сервер запущений

---

Успіхів у розробці! 🎉
